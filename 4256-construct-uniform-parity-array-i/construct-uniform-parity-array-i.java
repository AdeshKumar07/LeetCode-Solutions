class Solution {
    public boolean uniformArray(int[] nums1) {
        // reason because as we know given number in range of 1 to n means odd is also presnent so 
        // we can say if we subtract even-odd then we alway get a odd number , so its always true 
       return true;
        
    }
}